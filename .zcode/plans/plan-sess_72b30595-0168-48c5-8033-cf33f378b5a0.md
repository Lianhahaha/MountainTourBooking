## Problem
1. The 3 vertical cards in the **Trips** section look identical because `Trips.tsx:28` does `const trip = scheduled[0];` — every session card renders the **same** trek (Mt. Apo Summit), so only the date/time/slots differ while the image, title, location, price, and difficulty repeat 3 times.
2. You also want the **"LA" logo box + "LAAG TA ORB"** branding block (top-left of the header, above the cards) removed.

## Changes

### 1. Redesign the Trips session cards (`src/components/Trips.tsx`)
Make the cards read as **"dates of one trek"** instead of 3 identical trip cards:

- Show the trek's shared info **once** at the top of the card grid: a single header with the trek image, title, location, difficulty badge, duration, and price (no longer repeated per session).
- Below it, render the session **cards** (still up to 3) as **compact date cards** — each showing date, time, slots remaining, optional notes, and a **Book** button. No big repeated image, no repeated title/price/difficulty.
- Reuse existing helpers (`formatDate`, `formatPrice`, `difficultyColor`, `getSessionSlotsRemaining`) — no new utilities.
- Keep the fallback branch (no sessions → the wide "No dates yet" card) intact.
- **Private Group** card stays untouched.

### 2. Remove the header branding block (`src/components/Header.tsx:24-31`)
Remove the `<span>` "LA" logo box and the "LAAG TA ORB" text from the top-left header link.

> ⚠️ **Decision point:** Removing *both* leaves the top-left of the header empty. My recommendation is to **remove the "LA" square box but keep a plain text wordmark** so the logo-on-home navigation still works and the header isn't blank. If you'd rather remove *both* the box and the text entirely (empty top-left), say so when reviewing and I'll do that instead.

### Files touched
- `src/components/Trips.tsx` (redesign session card grid)
- `src/components/Header.tsx` (remove brand block)

No data, types, or other components change.